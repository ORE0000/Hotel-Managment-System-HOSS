import HOSSBookings from "../components/HOSSBookings";

const HOSSBookingsPage: React.FC = () => {
  return (
    <div className="min-h-screen bg-[var(--bg-primary)] w-full">
      <HOSSBookings />
    </div>
  );
};

export default HOSSBookingsPage;